# VGGT N-view pose on RealEstate10K — VGGT Table 1 paper-match.
#
# Targets VGGT Table 1 (Wang et al. 2025, arXiv:2503.11651), "Re10K (unseen)"
# column, "Ours (Feed-Forward)" row, AUC@30 = 0.853. Same PoseDiffusion
# protocol as the CO3Dv2 pose cell. New DATASET coverage on the pose axis.

name: vggt-realestate10k-pose
protocol: realestate10k_pose

model:
  name: vggt
  kwargs: {}

dataset:
  name: realestate10k-pose-eval
  kwargs:
    num_frames: 10
    seed: 0
    # clips: null   # set to the canonical 1.8K RealEstate10K test split
    #               # (DUSt3R/PoseDiffusion) to match the paper subset exactly

tasks: [pose]
scale_alignment: none
pointcloud_alignment: none
max_views: 10
device: cuda:0

pose_translation_antipodal: true
pose_auc_mode: vggt_co3d_histogram
pose_auc_thresholds: [3.0, 5.0, 15.0, 30.0]
pose_acc_thresholds: [5.0, 15.0]

paper_reference:
  primary_metric: pairwise_pose_auc@30
  value: 0.853
  tolerance_relative: 0.05
  citation: "Wang et al. 2025, VGGT (arXiv:2503.11651), Table 1, 'Re10K (unseen)' column, 'Ours (Feed-Forward)' row = 85.3 → AUC@30 0.853. Verified by ar5iv PDF read 2026-05-28 (the CO3Dv2 88.2 in the same row confirms the right cell)."
  source_confidence: verified_pdf

notes: >
  CROSS-MODEL LOW-PASS CONTROL — the fix is VGGT-SPECIFIC, not a generic
  quality knob (2026-06-11, GTX 1080 Ti, fp32 for VGGT; IDENTICAL `re10k_sub50`
  50-clip subset, same `lowpass_sigma` loader knob + pose recipe + AUC metric;
  drivers scripts/_re10k_lowpass_sweep.py, JSONs runlogs/{dust3r,mast3r}_re10k_lowpass_20260610.json).
  The native low-pass below shows a sigma=1.5 Gaussian recovers 82% of VGGT's
  gap — but that alone can't tell whether the low-pass is a TRUE VGGT-HF-fix or
  just a generic "blur helps everything" artifact of the metric/data. This
  control settles it by running the SAME sigma=1.5 low-pass through the two
  correspondence-based models that already CLEAR their RE10K targets natively:
    model    native(sigma=0)  sigma=1.5   dAUC@30   trans_cos(0->1.5)
    VGGT     0.6724           0.8205      +0.148    13.56 -> 7.80 deg
    MASt3R   0.8452           0.8241      -0.021     7.60 -> 8.16 deg
    DUSt3R   0.6556           0.5896      -0.066    14.78 -> 20.54 deg
  Both sigma=0 points reproduce the cross-model control EXACTLY (MASt3R 0.8452,
  DUSt3R 0.6556 — apparatus gate). The low-pass moves VGGT and the two matchers
  in OPPOSITE directions: it HELPS VGGT strongly (+0.148) while DEGRADING both
  MASt3R (-0.021) and DUSt3R (-0.066, trans_cos +5.8 deg). So the sigma=1.5
  prefilter is NOT a generic input-quality lever — VGGT's feed-forward
  translation regression is uniquely rescued by removing high-frequency content,
  whereas correspondence matching NEEDS that HF texture (blurring it removes the
  features it triangulates on). RRA@15 stays ~0.99 for all three (the entire
  effect is translation, never rotation), matching the documented gap shape.
  This CONFIRMS the HF-sensitivity is VGGT-specific and validates the anti-alias
  prefilter as a sound VGGT-only fix — closing the control the resolution sweep
  flagged as missing (the abandoned per-model MASt3R sweep). Diagnostic only —
  NOT a paper recipe.
  ---
  NATIVE-RES LOW-PASS CONFIRMS THE FIX (2026-06-10, GTX 1080 Ti, fp32,
  `re10k_sub50` 50-clip; new `lowpass_sigma` loader knob, driver
  scripts/_re10k_lowpass_sweep.py, JSON runlogs/vggt_re10k_lowpass_20260610.json
  on anima). The downsample sweep below confounded two effects — HF-band removal
  vs the bicubic-down/VGGT-up RESIZE ROUND-TRIP — so it could not prove the gap
  is spatial-frequency-driven. This sweep isolates it: a Gaussian blur applied
  IN PLACE at native 640px (same pixel count, aspect, and VGGT internal 518
  resize path as native; ONLY the high-frequency band attenuated). It traces a
  clean INVERTED-U that PEAKS ABOVE the downsample proxy:
    sigma(px)  0.0(native) 0.5    1.0    1.5(PEAK) 2.0    3.0
    AUC@30     0.6724      0.6758 0.7181 0.8205    0.8179 0.7765
    trans_cos  13.56       13.57  11.78  7.80      8.33   9.56
  RRA@15 stays ~0.99 throughout (rotation untouched). sigma=0 reproduces the
  recorded native 0.6724 EXACTLY (apparatus gate; sub50 reconstructed as the
  first-50-sorted-usable clips of the 1800-root). The sigma=1.5 peak 0.8205
  recovers **82%** of the 0.6724->0.853 gap — MORE than the downsample proxy's
  0.7731/64%, and ENTIRELY on translation. Because pixel count, aspect, and the
  resize path are all held at native, this RULES OUT the resize round-trip and
  pixel-count as the lever: the gap is purely HARMFUL HIGH-FREQUENCY CONTENT
  (YouTube re-encode blocking / aliasing) that VGGT's feed-forward translation
  regression is sensitive to and a low-pass removes. The actionable fix is a
  ~sigma 1.5 anti-alias PREFILTER on the staged ~480p frames (NOT a re-scrape,
  NOT a resolution change). Diagnostic only — NOT a paper recipe (pose GT is
  resolution-independent; protocol uses no GT focals).
  ---
  RESOLUTION SWEEP — the VGGT gap is INPUT-ARTIFACT, not low-resolution
  (2026-06-09, GTX 1080 Ti, fp32, `re10k_sub50` 50-clip; new
  `downsample_long_side` loader knob, same pose recipe; the native point
  reproduces the cross-model control's 0.6724 EXACTLY, validating the
  apparatus). BICUBIC-downsampling the staged 360x640 frames BEFORE VGGT's
  internal 518-wide resize traces a clean INVERTED-U in AUC@30, peaking well
  below native resolution:
    long-side  640(native) 384    288(PEAK) 192    128    96
    AUC@30     0.6724      0.7141 0.7731    0.6846 0.4960 0.4359
    trans_cos  13.56       11.45  9.69      13.58  19.38  22.77
  RRA@15 stays ~0.99 until 128 (then rotation also collapses as 128x72 loses
  real geometry). The 288 peak recovers ~64% of the 0.6724->0.853 gap, ENTIRELY
  on translation — matching the documented gap shape. Since VGGT resizes to 518
  either way, the gain from PRE-downsampling means our native YouTube-scraped
  frames carry HARMFUL high-frequency content (re-encode blocking / aliasing)
  that a low-pass removes; the residual gap is spatial-frequency/artifact-driven,
  NOT pixel-count-driven. This OVERTURNS the prior "feed VGGT native ~720p"
  suspect (the blocked re-scrape) — higher source res would add MORE artifacts,
  not fewer; the lever is a low-pass / anti-alias prefilter. VGGT-SPECIFICITY:
  the cross-model control already shows MASt3R/DUSt3R EXCEED their RE10K targets
  on these exact clips while VGGT alone misses (correspondence matching is robust
  to the frequency content; VGGT's feed-forward translation regression is not).
  A per-model MASt3R resolution control (does it ALSO peak below native, or stay
  flat?) was attempted but abandoned: MASt3R `sparse_ga` runs a 2-stage global
  alignment ~90 s/clip on the 1080 (~75 min for one res=96 point), too costly
  for confirmation the native cross-model control already provides. Diagnostic
  only — NOT a paper recipe (pose GT is
  resolution-independent, protocol uses no GT focals). Driver:
  scripts/_re10k_resolution_sweep.py.
  ---
  ⚠️ RAN at 1.8K scale on RTX 3090 (2026-06-07, bf16, 1800/1800): AUC@30 0.7288
  vs paper 0.853 (-14.6%, MISMATCH at tol 5%). Subset variance DISPROVEN (50→1800
  moved it only +1%). Gap is stable + entirely TRANSLATION (rot near-perfect
  RRA@15 0.998 / 1.20°; trans_cos ~10.8° stable).
  ---
  CROSS-MODEL CONTROL (2026-06-08, GTX 1080 Ti, IDENTICAL 50-clip subset
  `re10k_sub50` = first-50-sorted; the decisive test). Same loader+metric+data,
  three models:
    MASt3R  AUC@30 0.8452  (target 0.764, +11% — EXCEEDS, trans_cos 7.6°)
    DUSt3R  AUC@30 0.6556  (target 0.612,  +7% — EXCEEDS, trans_cos 14.8°)
    VGGT    AUC@30 0.6724  (target 0.853, -21% — MISSES, trans_cos 13.6°, fp32)
  **This OVERTURNS the earlier "data-fidelity" conclusion.** Both optimization-
  based models CLEAR their RE10K targets on the exact same ≤480p scraped clips,
  so the data is NOT a blanket limiter. The published ranking
  (VGGT 0.853 > MASt3R 0.764 > DUSt3R 0.612) INVERTS on our data (MASt3R top,
  VGGT bottom) — VGGT is the lone outlier. The gap is therefore VGGT-SPECIFIC ON
  RE10K, not generic data and not a found adapter bug (VGGT-CO3Dv2 0.8964 ✓,
  preprocessing mirrors upstream mode=crop, GT/metric validated by the two
  passing models here). WHY VGGT specifically (narrowed 2026-06-09): two of
  three hypotheses ruled out — (i) FRAME SAMPLING ruled out: MASt3R/DUSt3R EXCEED
  their targets on our exact 16-frame sampling, so it isn't harder than the
  paper's; (ii) WIDE ASPECT ruled out: the paper ran native RE10K through the
  same crop→518×wide and still got 0.853. PRIME SUSPECT = (iii) RESOLUTION: our
  scrape caps at ≤480p YouTube re-encodes (upscaled to 518); MASt3R/DUSt3R
  correspondence-matching is robust to soft frames, VGGT's feed-forward
  translation REGRESSION is not. Fix = feed VGGT native ~720p frames. The
  disambiguating re-scrape (native-res, or a dense/tight-baseline control) is
  BLOCKED: heavy scraping tripped YouTube bot-detection on anima's IP ("Sign in
  to confirm you're not a bot") — needs cookies or a cooldown.
  ---
  CO3Dv2 cross-check (still valid, rules out a GLOBAL adapter/harness bug): SAME
  VGGT adapter + AUC mode + antipodal handling reproduces CO3Dv2 0.8964 vs 0.882;
  SAME loader invert_pose/metric reproduces DUSt3R-CO3Dv2 0.789 vs 0.772 (a
  `[Rᵀ|−t]` inversion bug would have broken both); RE10K rotation perfect
  validates its GT parse. So the code is right in general — the failure is the
  VGGT×RE10K-recipe interaction above.
  ---
  History: 50-clip run first gave AUC@30 0.7184 (same shape); the 1.8K re-scrape
  + run was to test subset variance (ruled out). New RealEstate10K dataset
  coverage on the pose axis via the `realestate10k-pose-eval` loader (mirrors the
  CO3Dv2 pose loader). Frames scraped from YouTube (many source videos offline,
  92% hit) to $REALESTATE10K_ROOT as <clip_id>/{*.txt,<timestamp>.jpg}.
  Aggregation caveat as the CO3Dv2 pose cell (per-sample vs per-group, ~1-2%).
