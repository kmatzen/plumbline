# Depth Anything 3 (Large) on Sintel (MoGe HF eval bundle)
#
# DA3 paper Table 4: Sintel δ₁ = 0.755 (75.5) for the public DA3 release —
# notably DA3's weakest δ₁ benchmark. scale_shift_clamped (MoGe Sintel bundle,
# like da-v2-large-sintel). Target via ar5iv (NYU row cross-checks 0.974);
# PDF re-verify pending before ✅.
name: da3-sintel
protocol: sintel_moge
model:
  name: depth-anything-3
  kwargs:
    variant: large
    checkpoint: depth-anything/DA3-LARGE-1.1
scale_alignment: scale_shift_clamped
device: cuda:0
paper_reference:
  primary_metric: delta_1
  value: 0.755
  tolerance_relative: 0.02
  citation: "Bytedance Seed 2025, Depth Anything 3, Table 4. Sintel, δ₁ (scale+shift aligned)."
  source_confidence: approximate
notes: >
  DA3 vendored — no install step. Protocol mirrors da-v2-large-sintel-moge.
  DA3 eval protocol for Sintel is a hypothesis; confirm protocol + PDF target.
